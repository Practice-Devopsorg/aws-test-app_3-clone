{
  "name": "pvfleet-server",
  "version": "1.0.0",
  "description": "pvFLEET-server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "pm2 start index.js --name backend",
    "compose:up": "docker-compose -f ../docker-compose.yml up -d",
    "compose:down": "docker-compose -f ../docker-compose.yml down"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "mysql2": "^3.6.5",
    "nodemon": "^3.0.2",
    "pm2": "^5.2.0"
  }
}

